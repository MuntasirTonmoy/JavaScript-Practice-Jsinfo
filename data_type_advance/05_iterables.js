// What is iterables?

// How symbol.iterator method works in an object? what is an iterator? what next() method return in an iterator?

// const range = {from: 1, to: 5}, make the object iterable.

// is string iterable? is it works with surrogate pairs?

// What happened when we call an iterator explicitly? What is the benefit of it?

// is an iterable and an array like are same object? what is an array like object?

// How can we use all the array method in an iterable or array like object? What can we do with the optional mapping function in the Array.from()?

// how can we create a surrogate aware slice() for string?
