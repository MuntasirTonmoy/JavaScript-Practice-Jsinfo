// If we create our own data structure should we implement keys(), values(), entries()?

// keys(), values(), entries() are supported by Map, Set, Array, what is the difference with Object.keys(), Object.values(), Object.entries()?

// Object.keys(), Object.values(), Object.entries() return what?

// Object.keys/values/entries ignore symbolic properties?

// How can we convert an object to an array and convert it back to an object again?
