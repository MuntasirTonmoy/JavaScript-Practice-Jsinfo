// How to create an async function? An async function always return what? If we return regular values from async function what happened?

// How 'await' keyword works? Where it can be used? it returns what? Can we use 'await' keyword in regular function?

// Can we use top level 'await' in modern browser? How can we achieve that in old browser?

// Can we use 'thenable' object with 'await' keyword? What 'await' do when it gets a non promise object?

// How can we declare an async class method?

// If a promise reject how 'await' works? How We should handle error when using 'await'?

// If we use 'await' and don't use try...catch then what happened when we call that async function?

// If we forgot to add .catch() to the async function call how can we catch those errors?

// When we use async/await do we need to use .then() or .catch? What should we use?

// Does async/await works with Promise.all?
