// When a promise reject what happened?

// What is the easiest way to catch all errors in a promise chain?

// If we throw an error or any other error happened in the executor function is .catch() block catch it? How it works? Does promise handler works the same way?

// Does .catch() handler catch all the error? If we throw error inside .catch() handler then what happened? If we handler the error in .catch() then what happened?

// If we don't handle error in a promise handler then what happened?

// What js do for unhandled error? what does 'unhandledrejection' event do in browser? What event does it get?

// How to track unhandled error in Node.js?
