// every call to .then() return what? When a promise handler return a value it becomes the result of what?

// If we add multiple .then() handler to a single promise is it promise chaining?

// If a handler create and return a promise then further handler do what?

// Should we add .then() directly to a sequentially loaded promise or should we use chaining? When we can use nested promise handler?

// What is a thenable object? can a handler return thenable object? When returning a thenable object is useful? When a handler return an object JS check what?

// Why an async action should always return a promise?

// When a handler return a promise rest of the chain does what?
