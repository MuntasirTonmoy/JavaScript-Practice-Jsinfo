// what is type coercion and type conversion?

//* String
// When string coercion happens? How to convert data to string type explicitly?
// what conversion binary + operator does when one of the operand is string?

//* Number
// what are numeric conversion rules? undefined -> ? null -> ? true,false -> ? string -> ?
// When numeric coercion happens?
// what does unary + operator do in case of type conversion?

//* Boolean
// when boolean type coercion happens?
// what are the boolean conversion rules?
// double not !! convert the value to what data type?
