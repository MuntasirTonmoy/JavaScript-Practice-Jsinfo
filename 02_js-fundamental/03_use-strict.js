// what does 'use strict' does?

// where should we put 'use strict'? how can we enable strict mode inside a function only?

// How can we use "use strict" in browser console?

// If a browser don't support "use strict" how can we enable it?

// if we don't put "use strict" at the top of our script is it activated?

// Should we use "use strict" in modern js feature like module, Class?
