// why we use function? what is the main purpose of it?
// how to declare a function?
// what is parameter and argument in function?
// inside the function do we have full access to outer variable?
// when the local variable shadowed the outer variable?

// the value we passed via parameter, where it goes? if we change that value then what happened?
// how to pass default value for parameter? and when does the default value triggered?
// does the default value can be expression?
// how default param was given in old js? what is the alternative to default parameter?

// how to return a value from a function? when the execution reaches return statement what happened then?
// does a function without return statement return anything?

// how should we name a function ?
// one function one action
// function == comment
