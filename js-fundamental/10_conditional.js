//* if/else
// how if statement works? can if statement condition be an expression?

// does if statement convert its expression to boolean type implicitly?

// for one line if statement do we need to wrap it with bracket?

// when we use 'else if' and 'else' clause?

//* switch
// When a switch statement is useful?

// syntax of switch. How switch works?

// can we use expression as switch or case argument?

// How switch argument is checked with case argument? Is is use regular or strict equality?

// if we don't use break then what happened?

// how to group multiple case? the ability of grouping case is a side effect of what?

// Is the switch argument and case argument must be same type?
