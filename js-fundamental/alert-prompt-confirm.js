// what does alert() do in browser?

// what does prompt('title',[default]) do in browser? if we provide default value what happened?
// if user ignore the prompt what value it returns?

// what does confirm() use for in browser?
// what does confirm() return if user interact with it?
