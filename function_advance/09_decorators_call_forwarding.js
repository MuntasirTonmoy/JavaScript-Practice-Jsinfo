// what is a decorator? what does wrapper function do in a decorator function?

// When we pass an object method as value and then call that value what happened?

// if a function has properties will the decorated function keep access to those properties?

// what is the way to create decorators that keep access to original function properties?

//* call forwarding
// How we can call a function explicitly setting the context of 'this' keyword?

// how function.call() works? what it does? how we pass argument in it? can we use spread syntax?

// how function.apply() works? how we pass arguments in it? difference between .call() vs .apply()?

// how method borrowing works?

// when we run a function in forEach what is 'this' value by default?
