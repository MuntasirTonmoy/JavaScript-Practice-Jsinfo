// What is global object? what is global object for browser and node.js?

// in js what is the standardize name for global object?

// does browser global functions and var variable added to the global object? why this happens?

// is let, const, class, function expression added to global object?

// in js module function declaration or var variable added to global object?

// If we want to add an important value globally how should we do it? Should we use more global variable?

// When we should use global object? polyfill
