//* rest parameter
// can we call a function with any number of arguments even if it isn't defined? if we passed extra arguments in a function how can we get them?

// what is rest parameter syntax? how it works? must we use rest parameter at the end of parameter list?

// inside a function what is 'arguments'? is it an array? how it contains all the arguments?

// what is the downside of using 'arguments' instead of rest parameter?

//* spread syntax
// what is spread syntax? how it works?

// what are the different styles of using spread syntax? multiple iterator, combining with normal values, merging arrays.

// How spread syntax internally works for iterator?

// Difference between Array.from(iterator) vs spread syntax? How can we copy an array using spread syntax?

// Can we use spread syntax in object? how it works for object? can we copy an object using spread syntax?

//=============================================================================================
// Spread syntax (...) allows an iterable such as an array expression or string to be EXPANDED in places where zero or more arguments (for function calls) or elements (for array literals) are expected.

// In an object literal, the spread syntax enumerates the properties of an object and adds the key-value pairs to the object being created.
